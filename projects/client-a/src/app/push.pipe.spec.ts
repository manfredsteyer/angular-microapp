/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PushPipe } from './push.pipe';

describe('Pipe: Pushe', () => {
  it('create an instance', () => {
    let pipe = new PushPipe();
    expect(pipe).toBeTruthy();
  });
});
